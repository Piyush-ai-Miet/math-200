You are given parameters of an RSA encryption: the modulus n = 12407072677633161347, the public exponent e = 65537, and the ciphertext c = 6680131599371095691. Your task is to decrypt the ciphertext c to recover the original message, assuming you can factor n and compute the private key.

Steps:
1. Factor the modulus n into its prime factors p and q.
2. Compute the totient \(\phi(n) = (p-1)(q-1)\).
3. Compute the private exponent d as the modular inverse of e modulo \(\phi(n)\).
4. Decrypt the ciphertext c by computing \(m = c^d \mod n\).
5. Convert the numeric message m to a string to recover the readable plaintext.

Output Format:
Provide the decrypted message in the format FLAG{...}, replacing the ellipsis with the recovered plaintext. Ensure the output is exactly: FLAG{recovered_message}

Do not include any additional explanation or steps, just output the flag.
